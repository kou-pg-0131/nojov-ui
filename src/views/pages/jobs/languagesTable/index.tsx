import React from 'react';
import { Job } from '../../../../domain';

type Props = {
  jobs: Job[];
  showLink: boolean;
};

export const Table: React.FC<Props> = (props: Props) => {
  return (
    <div>hello</div>
  );
};
